<div class="absolute inset-0 bg-cover bg-center bg-no-repeat"
       data-projects-dashboard-target="background"
       style="background-image: url('<%= asset_path(@projects.first[:background_path]) %>'); transition: opacity 0.3s ease-in-out;">
    <!-- Dark Overlay -->
    <div class="absolute inset-0 bg-gradient-to-b from-black/70 via-black/50 to-black/80"></div>
  </div>
